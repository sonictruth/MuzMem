'use strict';

/**
 * @ngdoc function
 * @name muzpuzApp.controller:GameCtrl
 * @description
 * # GameCtrl
 * Controller of the muzpuzApp
 */
 angular.module('muzpuzApp')
 .controller('GameCtrl', ['$scope',function GameCtrl($scope) {
 	console.log($scope);
 }]);

